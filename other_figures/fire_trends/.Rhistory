mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.2, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
# guides(color = guide_legend(title = 'EPA Eco-Region'), linetype = guide_legend(title = 'EPA Eco-Region')) +
ylab('Burned Area (ha)') + xlab('Year')
p2
ggsave(filename = 'Fig1_FRAP_time_series_South_Sierra.png', height=10, width=16, units = 'cm', dpi=900)
lines <- c("Wildfire" = "solid", "RxBurn" = "dotted")
#Create a time series of FRAP burned area
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.2, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
# guides(color = guide_legend(title = 'EPA Eco-Region'), linetype = guide_legend(title = 'EPA Eco-Region')) +
ylab('Burned Area (ha)') + xlab('Year')
p2
lines <- c("Wildfire" = "dashed", "RxBurn" = "dotted")
#Create a time series of FRAP burned area
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.2, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
# guides(color = guide_legend(title = 'EPA Eco-Region'), linetype = guide_legend(title = 'EPA Eco-Region')) +
ylab('Burned Area (ha)') + xlab('Year')
p2
lines <- c("Wildfire" = "dashed", "RxBurn" = "solid")
#Create a time series of FRAP burned area
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.2, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
# guides(color = guide_legend(title = 'EPA Eco-Region'), linetype = guide_legend(title = 'EPA Eco-Region')) +
ylab('Burned Area (ha)') + xlab('Year')
p2
ggsave(filename = 'Fig2_FRAP_time_series_South_Sierra.png', height=10, width=16, units = 'cm', dpi=900)
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
# guides(color = guide_legend(title = 'EPA Eco-Region'), linetype = guide_legend(title = 'EPA Eco-Region')) +
ylab('Burned Area (ha)') + xlab('Year')
p2
ggsave(filename = 'Fig2_FRAP_time_series_South_Sierra.png', height=10, width=16, units = 'cm', dpi=900)
lines <- c("Wildfire" = "dotted", "RxBurn" = "solid")
#Create a time series of FRAP burned area
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
# guides(color = guide_legend(title = 'EPA Eco-Region'), linetype = guide_legend(title = 'EPA Eco-Region')) +
ylab('Burned Area (ha)') + xlab('Year')
p2
ggsave(filename = 'Fig2_FRAP_time_series_South_Sierra.png', height=10, width=16, units = 'cm', dpi=900)
lines <- c("Wildfire" = "dashdot", "RxBurn" = "solid")
#Create a time series of FRAP burned area
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
# guides(color = guide_legend(title = 'EPA Eco-Region'), linetype = guide_legend(title = 'EPA Eco-Region')) +
ylab('Burned Area (ha)') + xlab('Year')
p2
lines <- c("Wildfire" = "dotdash", "RxBurn" = "solid")
#Create a time series of FRAP burned area
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
# guides(color = guide_legend(title = 'EPA Eco-Region'), linetype = guide_legend(title = 'EPA Eco-Region')) +
ylab('Burned Area (ha)') + xlab('Year')
p2
ggsave(filename = 'Fig2_FRAP_time_series_South_Sierra.png', height=10, width=16, units = 'cm', dpi=900)
#USFS Fire severity
fire.sev <- read_sf(file.path(sev_in, "VegBurnSeverity18.shp"))
sev_in <- "D:\\Large_Files\\USFS\\Fire_Severity\\"
#USFS Fire severity
fire.sev <- read_sf(file.path(sev_in, "VegBurnSeverity18.shp"))
sev_in <- "D:\\Large_Files\\USFS\\Fire_Severity"
#USFS Fire severity
fire.sev <- read_sf(file.path(sev_in, "VegBurnSeverity18.shp"))
sev_in <- "D:\\Large_Files\\USFS\\Fire_Severity\\VebBurnSeverity18_1_shp"
#USFS Fire severity
fire.sev <- read_sf(file.path(sev_in, "VegBurnSeverity18.shp"))
#Add date to Fire Severity data
summary(fire.sev)
fire.sev$FIRE_YEAR
#Join the Fire Severity and South Seierra Region
fire.sev.sierra <- st_join(fire.sev, usfs.sierra.sf)
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = BURNSEV, linetype = BURNSEV), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>%
group_by(FIRE_YEAR, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = BURNSEV, linetype = BURNSEV), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = BURNSEV, linetype = BURNSEV), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = BURNSEV, linetype = BURNSEV), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
# scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
# scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = BURNSEV, linetype = BURNSEV), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
# scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
# scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = BURNSEV), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
# scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
# scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
# scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
# scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour(name="Fire Severity", values = c(1 = 'Lowest', 2 = 'Low', 3 = 'Moderate', 4 = 'High'), aesthetics = 'color') +
scale_linetype(name="Fire Type (FRAP)", values = c(1 = 'Lowest', 2 = 'Low', 3 = 'Moderate', 4 = 'High')) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour(name="Fire Severity", values = c('1' = 'Lowest', '2' = 'Low', '3' = 'Moderate', '4' = 'High'), aesthetics = 'color') +
scale_linetype(name="Fire Type (FRAP)", values = c('1' = 'Lowest', '2' = 'Low', '3' = 'Moderate', '4' = 'High')) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_color_discrete(name="Fire Severity", values = c('1' = 'Lowest', '2' = 'Low', '3' = 'Moderate', '4' = 'High'), aesthetics = 'color') +
scale_linetype_discrete(name="Fire Severity", values = c('1' = 'Lowest', '2' = 'Low', '3' = 'Moderate', '4' = 'High')) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471, color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_color_discrete(name="Fire Severity", labels = c('Lowest', 'Low', 'Moderate', 'High'), aesthetics = 'color') +
scale_linetype_discrete(name="Fire Severity", labels = c('Lowest', 'Low', 'Moderate', 'High')) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p3 <- ggplot() +
#Line of total USFS burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471,
color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Line of total USFS burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/2.471 ),
color = 'black', alpha = 0.4, size = 1.5) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_color_discrete(name="Fire Severity", labels = c('Lowest', 'Low', 'Moderate', 'High'), aesthetics = 'color') +
scale_linetype_discrete(name="Fire Severity", labels = c('Lowest', 'Low', 'Moderate', 'High')) +
ylab('Burned Area (ha)') + xlab('Year')
p3
p1 + (p2 / P3)
p1 + (p2 / p3)
p3 <- ggplot() +
#Line of total USFS burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/10000,
color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Line of total USFS burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/10000),
color = 'black', alpha = 0.4, size = 1.5) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_color_discrete(name="Fire Severity", labels = c('Unchanged', 'Low', 'Moderate', 'High'), aesthetics = 'color') +
scale_linetype_discrete(name="Fire Severity", labels = c('Unchanged', 'Low', 'Moderate', 'High')) +
ylab('Burned Area (ha)') + xlab('Year')
p3
ggsave(filename = 'Fig3_USFS_Fire_Severity_time_series_South_Sierra.png', height=10, width=16, units = 'cm', dpi=900)
frap$Area.Sf <- st_area(frap)
frap
frap$Area.Sf
rxburn$Area.Sf <- st_area(rxburn) #m^2 units
fire.sev.sierra$Area.sf <- st_area(fire.sev.sierra)
frap$Area.sf <- st_area(frap) #m^2 units
rxburn$Area.sf <- st_area(rxburn) #m^2 units
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(Area.sf)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/10000, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(Area.sf)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/10000, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p2
fire.sev$Area.sf <- st_area(fire.sev)
frap.sierra <- st_join(frap[frap$valid == TRUE, ], usfs.sierra.sf)
#Get the prescribed burn polygons in the South Sierra USFS Regions
rxburn.sierra <- st_join(rxburn, usfs.sierra.sf)
#Join the Fire Severity and South Seierra Region
fire.sev.sierra <- st_join(fire.sev, usfs.sierra.sf)
cols <- c("Wildfire"="black","RxBurn"="gray")
lines <- c("Wildfire" = "dotdash", "RxBurn" = "solid")
#Create a time series of FRAP burned area
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(Area.sf)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/10000, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(Area.sf)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/10000, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p2
p3 <- ggplot() +
#Line of total USFS burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Area.sf)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/10000,
color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Line of total USFS burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, .groups = 'keep') %>% summarize(Area = sum(Area.sf)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/10000),
color = 'black', alpha = 0.4, size = 1.5) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_color_discrete(name="Fire Severity", labels = c('Unchanged', 'Low', 'Moderate', 'High'), aesthetics = 'color') +
scale_linetype_discrete(name="Fire Severity", labels = c('Unchanged', 'Low', 'Moderate', 'High')) +
ylab('Burned Area (ha)') + xlab('Year')
p3
frap.sierra
fire.sev$Area.sf %>% as.numeric()
fire.sev$Area.sf
fire.sev$Area.sf %>% as.numeric()
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(as.numeric(Area.sf))),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/10000, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(Area.sf)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/10000, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p2
p2 <- ggplot() +
#Line of total FRAP burned area in the South Sierra
geom_line(data = as.data.frame(frap.sierra[frap.sierra$year >= 1850 & frap.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'Wildfire', linetype = 'Wildfire'), size = 1) +
#Line of total RxBurn burned area in the South Sierra
geom_line(data = as.data.frame(rxburn.sierra[rxburn.sierra$year >= 1850 & rxburn.sierra$year <= 2020, ]) %>%
group_by(year, .groups = 'keep') %>% summarize(Area = sum(GIS_ACRES)),
mapping = aes(x = as.Date(as.character(year), format = "%Y"), y = Area * 1/2.471, color = 'RxBurn', linetype = 'RxBurn'), size = 1) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_colour_manual(name="Fire Type (FRAP)",values=cols, aesthetics = 'color') +
scale_linetype_manual(name="Fire Type (FRAP)", values = lines) +
ylab('Burned Area (ha)') + xlab('Year')
p2
ggsave(filename = 'Fig2_FRAP_time_series_South_Sierra.png', height=10, width=16, units = 'cm', dpi=900)
p3 <- ggplot() +
#Line of total USFS burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, BURNSEV, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/10000,
color = as.factor(BURNSEV), linetype = as.factor(BURNSEV)), size = 1) +
#Line of total USFS burned area in the South Sierra
geom_line(data = as.data.frame(fire.sev.sierra) %>% filter(!is.na(BURNSEV) & BURNSEV != 255) %>%
group_by(FIRE_YEAR, .groups = 'keep') %>% summarize(Area = sum(Shape_Area)),
mapping = aes(x = as.Date(as.character(FIRE_YEAR), format = "%Y"), y = Area * 1/10000),
color = 'black', alpha = 0.4, size = 1.5) +
#Do the black and white theme
theme_bw() +
#Figure
theme(legend.position = c(0.15, 0.8), legend.background = element_rect(colour = NA, fill = NA),
legend.title = element_text(size = 8), legend.text = element_text(size = 6)) +
scale_color_discrete(name="Fire Severity", labels = c('Unchanged', 'Low', 'Moderate', 'High'), aesthetics = 'color') +
scale_linetype_discrete(name="Fire Severity", labels = c('Unchanged', 'Low', 'Moderate', 'High')) +
ylab('Burned Area (ha)') + xlab('Year')
p3
ggsave(filename = 'Fig3_USFS_Fire_Severity_time_series_South_Sierra.png', height=10, width=16, units = 'cm', dpi=900)
